import { DateRange, Event } from "../Types.js";
import { Recurrence } from "../dateRange/checkRecurrence.js";
export declare const expand: (dateRange: DateRange, recurrence: Recurrence, limit: number) => DateRange[];
export declare const expandEvent: (event: Event, limit: number) => Event[];
