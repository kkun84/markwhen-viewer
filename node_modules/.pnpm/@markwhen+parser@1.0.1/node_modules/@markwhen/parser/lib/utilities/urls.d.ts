import { DateTime } from "luxon";
import { Path, Eventy, Event } from "../Types.js";
export declare const toArray: (node: Eventy | undefined, cutoff?: DateTime) => {
    path: Path;
    event: Event;
}[];
export declare function mapUrls(events: {
    path: Path;
    event: Event;
}[]): {
    path: Path;
    event: Event;
    url: string;
}[];
