import { checkComments } from "./checkComments.js";
import { checkTags } from "./checkTags.js";
export function checkNonEvents(line, i, lengthAtIndex, context) {
    return [checkComments, checkTags].some((f) => f(line, i, lengthAtIndex, context));
}
